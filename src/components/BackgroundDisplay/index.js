import React from 'react';

import axios from 'axios';

export const BackgroundDisplay = ({ supplier }: { supplier: 'solid' | 'unsplash' | 'direct' | 'gradient' }) => {
    React.useEffect(() => {
        
    }, [])
}